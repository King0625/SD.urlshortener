version: "2"
sql:
  - engine: "postgresql"
    queries: "./internal/db/query"
    schema: "./migration"
    gen:
      go:
        package: "db"
        out: "./internal/db"
        sql_package: "pgx/v5"
        emit_empty_slices: true
        emit_json_tags: true